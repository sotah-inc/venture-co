{
  "name": "@sotah-inc/api-local-dev",
  "version": "1.0.9",
  "description": "",
  "main": "build/dist/index.js",
  "scripts": {
    "clean": "rimraf ./build",
    "debug": "run-p build:watch lint:watch start",
    "start": "nodemon --watch ./dist ./dist/index.js",
    "lint": "tslint ./src/**/*.ts{,x}",
    "lint:watch": "chokidar --silent ./src/**/*.ts{,x} -c \"npm run -s lint\"",
    "build": "tsc",
    "build:clean": "run-s clean build",
    "build:watch": "npm run -s build -- --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sotah-inc/api": "^1.0.9",
    "npm-run-all": "^4.1.5",
    "typeorm": "^0.2.18"
  },
  "devDependencies": {
    "@blueprintjs/tslint-config": "^1.8.0",
    "chokidar-cli": "^1.2.2",
    "nodemon": "^1.19.1",
    "prettier": "^1.18.2",
    "rimraf": "^2.6.3",
    "tslint": "^5.17.0",
    "typescript": "^3.5.2"
  }
}
