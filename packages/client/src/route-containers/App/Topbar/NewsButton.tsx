import { withRouter } from "react-router-dom";

import { NewsButtonContainer } from "../../../containers/App/Topbar/NewsButton";

export const NewsButtonRouteContainer = withRouter(NewsButtonContainer);
